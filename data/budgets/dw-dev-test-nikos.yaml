amount:
  units: 3.0
display_name: budget for dw-dev-test-nikos
filter:
  period:
    calendar: MONTH
  projects:
  - dw-dev-test-nikos
threshold_rules:
- percent: 0.5
- percent: 0.75
update_rules:
  default:
    disable_default_iam_recipients: true
    monitoring_notification_channels:
    - billing-default
